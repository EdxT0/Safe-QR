Prerequisites:
1. Download node.js
2. Download the safe-qr folder

To run the program locally:
Open Administrator command prompt
cd safe-qr

run the following commands:
npm install (if cannot try npm install html5-qrcode)
npm run dev


Open browser
http://localhost:3000

To deploy it on vercel
npm install -g vercel
vercel


